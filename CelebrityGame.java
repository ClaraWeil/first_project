/*class CelebrityGame
{
  protected ArrayList<Celebrity> celebGameList;
  private Celebrity gameCelebrity;
  //protected CelebrityFrame gameWindow; 
  
  public CelebrityGame(ArrayList<Celebrity> c)
  {
    celebGameList = c;
    //gameWindow = new CelebrityFrame(this); 
  }*/
  /** * Validates the name of the celebrity. The name of the celebrity must have at least * 4 characters to be considered valid. * @param name The name of the Celebriity * @return true if the supplied Celebrity is valid, false otherwise. */
  /*public boolean validateCelebrity(String name) 
  {
    if(name.length() >= 4)
    {
      return true;
    }
    return false;
  }*/
  /** * Checks that the supplied clue has at least 10 characters and/ot r * is a series of clues. * This method would be expanded based on your subclass of Celebritye . * @param clue The text of the clue(sc ) * @param type Supports a subclass of Celebrity (LiteratureCelebrityc ) * @return If the clue is valid  . */ 
  /*public boolean validateClue(String clue, String type) 
  {
    if(clue.length() >= 10)
    {
      return true;
    }
    return false;
  }
  public void addCelebrity(String name, String clue)
  {
    celebGameList.add(new Celebrity(name,clue));
  }
}*/